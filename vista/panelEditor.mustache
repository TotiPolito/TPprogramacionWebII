<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Panel del Editor</title>
    <link rel="stylesheet" href="{{BASE_URL}}/public/style/headerFooter.css">
    <link rel="stylesheet" href="{{BASE_URL}}/public/style/style.css">
    <link rel="stylesheet" href="{{BASE_URL}}/public/style/panelEditor.css">
</head>
<body>

<div class="form panel">
<h2>Bienvenido, {{usuario.nombre_completo}}</h2>
<p>Rol: <strong>{{usuario.rol}}</strong></p>

<section>
    <p>Preguntas reportadas pendientes: {{cantidadReportes}}</p>
    <p>Preguntas sugeridas por revisar: {{cantidadSugerencias}}</p>
</section>

<nav>
    <ul>
        <li><a style="color: blue" href="{{BASE_URL}}/index.php?controller=Reportes&method=listar">Ver preguntas reportadas</a></li>
        <li><a style="color: blue" href="{{BASE_URL}}/index.php?controller=PreguntaSugerida&method=listarPendientes">Ver sugerencias</a></li>
        <li><a style="color: red" href="{{BASE_URL}}/index.php?controller=Login&method=logout">Cerrar sesión</a></li>
    </ul>
</nav>
</div>

</body>
</html>